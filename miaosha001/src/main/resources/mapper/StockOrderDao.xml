<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.miaosha.miaosha001.dao.StockOrderDao">

    <resultMap type="com.example.miaosha.miaosha001.entity.StockOrder" id="StockOrderMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="sid" column="sid" jdbcType="INTEGER"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="userId" column="user_id" jdbcType="INTEGER"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="StockOrderMap">
        select
          id, sid, name, user_id, create_time
        from stock_order
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="StockOrderMap">
        select
          id, sid, name, user_id, create_time
        from stock_order
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="sid != null">
                and sid = #{sid}
            </if>
            <if test="name != null and name != ''">
                and name = #{name}
            </if>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
        </where>
        limit #{pageable.offset}, #{pageable.pageSize}
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from stock_order
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="sid != null">
                and sid = #{sid}
            </if>
            <if test="name != null and name != ''">
                and name = #{name}
            </if>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into stock_order(sid, name, user_id, create_time)
        values (#{sid}, #{name}, #{userId}, #{createTime})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into stock_order(sid, name, user_id, create_time)
        values
        <foreach collection="entities" item="com.example.miaosha.miaosha001.entity" separator=",">
        (#{entity.sid}, #{entity.name}, #{entity.userId}, #{entity.createTime})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into stock_order(sid, name, user_id, create_time)
        values
        <foreach collection="entities" item="com.example.miaosha.miaosha001.entity" separator=",">
            (#{entity.sid}, #{entity.name}, #{entity.userId}, #{entity.createTime})
        </foreach>
        on duplicate key update
        sid = values(sid),
        name = values(name),
        user_id = values(user_id),
        create_time = values(create_time)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update stock_order
        <set>
            <if test="sid != null">
                sid = #{sid},
            </if>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="userId != null">
                user_id = #{userId},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from stock_order where id = #{id}
    </delete>

</mapper>

